impl Solution {
    pub fn count_smaller(nums: Vec<i32>) -> Vec<i32> {
        
    }
}


#[cfg(test)]
mod tests {
    use crate::*;

    #[test]
    fn first() {
        let nums = vec![-1];
        let expected = vec![0];
        let ans = Solution::count_smaller(nums);
    }
}

struct Solution;
